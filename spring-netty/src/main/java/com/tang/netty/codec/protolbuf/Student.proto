syntax = "proto3";//版本
option java_outer_classname = "StudentPOJO";//生成外部类名，同时也是文件名
//protobuf是以message管理数据
message Student{//会在StudentPOJO外部类生成一个内部类Student，是真正发送的POJO对象
  int32 id = 1;//student类中的属性，1表示属性序号，不是值
  string name = 2;
}